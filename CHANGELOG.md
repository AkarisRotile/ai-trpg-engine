# 更新记录

按时间倒着写。只记用户能感觉到的东西，内部重构不占篇幅。

---

## 2026-09-19

### 插件系统

界面顶栏多了「🧩 插件」。插件就是 `data/plugins/<id>/` 里的一个文件夹，
丢进去、重新扫描、打开开关就能用。它能有自己的页面、悬浮窗，或者挂到右栏当标签页。

顺手写了两个当例子：

**思维链查看器**。跑团的时候看每个座位正在写什么。左边是模型的原话，
下面标出引擎把它改成了什么样。查「是哪一步把话写坏了」就靠这个。
它是真的流式，字是滚出来的。

**战斗格子**。守秘人在 `<state>` 里摆坐标，格子那边跟着动。
右边是 DEX 排出来的先攻顺序，下面一条追逐轨道。有立绘就贴立绘。

同一件事可以有好几个插件做，比如嫌官方这个格子难看，自己写一个占同一个坑位，
在管理页选一下就当值了。

写了 `docs/插件编写格式.md`，照着就能写。

### 立绘和头像

引擎只管存和取，不生成图。图你自己放，命名约定是 `pc_角色名` / `npc_名字` /
`player_玩家id`。放进去之后战斗格子和右栏都会用上，插件也能调。
图存在 `data/portraits/`，升级程序不会丢。

### 战斗格子与追逐

以前打架只能在日志里念，谁在哪全靠脑补。现在位置是摆出来的：
守秘人写 `place 余快 3 5`，格子上就有棋子。先攻按 DEX 自己排。

### 那个「自动暂停被当成结局」的 bug

之前点自动推进，跑到这一批的轮数上限就跳出「结局已过，模组已解禁」，
然后拉着全桌复盘一个连第一幕都没跑完的故事。

原因是往上限跑和真正结束走了同一条路。现在只有引擎判定该结束才算结束，
手动点停也不算。

### 每轮谁出场改成抽签

以前每一轮每个人都得动，一轮下来满桌都在说话，没有一个人是安静的。

现在按各人的桌边话量抽：话少的四轮里出一两次，话多的基本每轮都在。
有人点名提到你、或者守秘人标了这一轮是关键节点，那你必动。
一场戏也可能只是守秘人和某一个玩家一来一回，别人在旁边看。

### 说话的毛病

这一批改得最多。原来模型写出来的东西有几样特别刺眼：

破折号、「不是 A 而是 B」、Markdown 加粗、把属性串成一条念出来、
一句一行像写诗、为了一个比喻停下来半天、把自己那点选择交代得清清楚楚、
守秘人替玩家决定动作甚至替玩家开口说话。

没有歧义的（破折号、加粗、那个句式）现在由代码直接改掉，不指望模型重写。
剩下的写进了提示词，配了正例和反例。

顺带发现几件有意思的事：

桌边规矩以前**根本没进正式回合的提示词**，只在车卡和闲聊那两处生效。

「我把椅子挪了挪」这种一句一个「我把」的卡带，源头是我们自己举的例子里
三条有两条带「把」。提示词里举什么例子，模型就学成什么样。

有个守秘人的正例是「你可以提醒他们这一局大概会有很多要翻东西的场合」，
模型照抄成了「这局翻的场面不少」。等于守秘人开局就把本剧透了。

### 模组页打不开

模组一多，点「模组」要等八秒才出来，看着就像坏了。
实际是每次打开弹窗都要把所有模组的正文重读一遍。
现在加了缓存，启动的时候先在后台扫一遍，再点就是瞬间。

### 界面上有等待提示了

以前点了没反应就只能干等。现在慢过 400 毫秒会浮出一个小提示，
写清楚在等什么，比如「守秘人正在通读模组」。

### 开场时间

以前不管模组写的哪一年，开局一律是 1925 年 10 月 3 日晚上八点。

现在守秘人研读模组的时候自己挑一天，模组里写了日期就用日期，
没写就按季节、天气、电报上的日子挑一个。引擎拿它排日历。
同一本模组每次开局都是同一天，不同模组不一样。

### 梗

以前只有散场复盘的时候由模型自己申报。现在引擎自己数：
同一句短话被两个以上不同的人说过，就算梗，写进那个人的玩家卡里，
下一局开团他会记着。

模型看不见别人说过什么，所以这件事只能引擎来做。

---

## 更早

界面从无到有、车卡不再超支、名字改成 AI 自己取、研读室、
跨周目玩家记忆，这些在提交历史里。
