# 协作约定

给下一个读到这个文件的我，或者别的 AI 协作者。
项目本身看 `README.md`。这里只写两件事：**怎么说话**，**别踩哪些线**。

---

## 一、语言规范

这一节同时管两边：**我回用户话的方式**，和**游戏里 AI 角色说话的规矩**。
两边犯的是同一批毛病，因为都是同一个模型写的。

### 不用这些

**1.「不是 A，而是 B」全家**

不是…而是…／这不是…这是…／不是…是…／并非…而是…／与其说…不如说／
不在于…而在于…／拆成两句也算／A 不是 B，C 才是。

用户原话：「最凡人最恶心的那种句型，一出现就没任何兴致了」。

为什么它讨厌：靠否定一个没人说过的靶子来制造洞见，修辞上把话收死，
而且那是社论和广告的口气。

**2. 破折号**

想停顿就断句，想转折就另起一句。
用户原话：「现实说话不可能有人说得出破折号这种东西」。

**3. Markdown 加粗当强调**

真人打字不会加粗。要强调就把话本身写清楚。

**4. 三项并列、对仗、排比**

「谁先接话，谁低头看杯子，说到哪一句的时候手指会动一下」这种三拍子。
真人的观察是零碎的、不均匀的。
**禁的是结构，不是比喻。** 好比喻留着。

**5. 报账单**

把属性、技能、数字串成一条念出来。数字只在吐槽里出现，或者干脆别提。

**6. 前置声明**

「先说好」「提前交底」「说个实话：」。想说什么就直接说。

**7. 论证自己**

别交代算盘，理由留在心里。
用户原话：「如果做什么都要摆事实、讲依据的话，会非常没意思」。

**8. 撇清式表态**

「别指望我」「我不去」「别催」「谁挨打谁上」。
用户原话：「我是不喜欢和会这样说话的人交朋友的」。

自嘲可以，「我废了」是好句子。差别在人还在不在场。

### 下笔前问自己

- **这句话说出口，桌上的人愿不愿意跟你玩？**（用户原话）
- 我是不是在摆事实、讲依据？那样很没意思。
- 如果对方只学我这几句，会变成什么样的人？

### 回话本身也要守

- 短。能一句别用三句。
- 别写成审计报告。不要「证据一／证据二／证据三」，不要每次都附验证数据。
- 先给结论或观点，再给必要的背景。别铺三段才说到正题。
- 不确定就说不确定，不用给每个判断找依据。
- 表格不是不能用，但别拿它当主要表达方式。

---

## 二、项目红线

- `参考（严格禁止更新至github）/`：第三方预设。禁止二创、禁止入库、禁止进分享包。
  `.gitignore` 已经挡住。改完文字跑一次 `tools/leak_sentinel.py`。
- 绝不提交：`data/config.json`、`data/players/`、`data/memory/`、规则书 PDF、
  `dist/`、`build/`、`_internal/`、`COC跑团引擎.exe`。
  提交前跑 `tools/scan_secrets.py`，把令牌临时放进 remote URL 的那次记得还原。
- 改数字规则先看 `docs/车卡规范.md`。
  改文字先看 `docs/改版方案-桌边语气与时间.md`。
- 说完要有据：自检 `tools/selftest_offline.py`，真窗口 `tools/smoke_ui.py`，
  打包版 `tools/smoke_exe.py`。

---

## 三、这个项目的脾气

**引擎负责所有数字**：骰子、时间、技能点预算、伤害、理智。
AI 只负责说话和做选择。

凡是模型算不好的事，都由代码兜住，不要靠提示词求它。
这条已经踩实过三次：骰子、时间、车卡预算。

**提示词里的正例就是规格。** 举什么例子，模型就学成什么样。
所以写例子之前先问：如果它只学这几句，会变成什么。
